# Raffle handoff files (from the inspiring-brown session, 2026-09-18)

- `invitation-preevent.html` / `.txt`: the invitation email every invitee gets.
  `__RAFFLE_EXEC_URL__` stands for the raffle web-app exec URL (never committed).
  Marj sends this to the 502-person ABC list through Moosend; individual copies
  already went to two invitees by hand.
- `thank-referrer.html`, `introduce-agent.html`: FUB email templates 1 and 2 in
  TSG house style, to be created in the FUB UI (the API has no template
  collection). Merge field for the contact's first name: `%contact_first_name%`.
